# Run this file with `sudo`
# And make sure you give it executable permission with `chmod +x install.sh`

# Make timefetch executable
chmod +x timefetch

# Copy timefetch to the bin directory
cp timefetch /usr/local/bin

echo "Done"